name: miniprint
description: Honeypot untuk mendeteksi percobaan serangan printer
enabled: false  # disable
ports:
  - host: 9100
    container: 9100
env: {}
volumes:
- data/miniprint/log/:/opt/miniprint/log/
- data/miniprint/uploads/:/opt/miniprint/uploads/
