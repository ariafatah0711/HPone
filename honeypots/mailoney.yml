name: mailoney
description: SMTP honeypot to monitor and record email spam/bot activities
enabled: false
ports:
- host: 25
  container: 25
  description: "SMTP email transfer protocol"
# - host: 587
#   container: 25
#   description: "SMTP submission port (secure)"
env:
volumes:
- data/mailoney/log:/opt/mailoney/logs
