name: honeyaml
description: YAML-based honeypot to detect HTTP interactions and log attacker activities
enabled: false
ports:
- host: 8085
  container: 8080
  description: "HTTP web server (YAML-based)"
env: {}
volumes:
- data/honeyaml/log:/opt/honeyaml/log
